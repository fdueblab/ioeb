server {
    listen 80;
    server_name fdueblab.cn;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name fdueblab.cn;

    ssl_certificate /etc/nginx/ssl/fdueblab.cn_bundle.crt;
    ssl_certificate_key /etc/nginx/ssl/fdueblab.cn.key;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 后端API代理
    location /api/ {
        # 增加此行设置客户端请求体大小限制
        client_max_body_size 10M;  # 可以根据需求调整，此处设为10MB
        proxy_pass http://backend:5000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态文档
    location /docs/ {
        proxy_pass http://docs:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 项目1 API代理
    location /api/project1/ {
        # 增加此行设置客户端请求体大小限制
        client_max_body_size 10M;  # 可以根据需求调整，此处设为10MB
        proxy_pass http://project-1:5000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 项目3 API代理
    location /api/project3/ {
        proxy_pass http://project-3:5000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 项目4 API代理
    location /api/project4/ {
        # 增加此行设置客户端请求体大小限制
        client_max_body_size 10M;  # 可以根据需求调整，此处设为10MB
        proxy_pass http://project-4:5000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # linezolid API代理
    location /api/linezolid/ {
        # 增加此行设置客户端请求体大小限制
        proxy_pass http://linezolid:5000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # linezolid gradio代理
    location /app/linezolid/ {
        # 增加此行设置客户端请求体大小限制
        proxy_pass http://linezolid:7860/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # linezolid mcp代理
    location /mcp/linezolid/ {
        # 增加此行设置客户端请求体大小限制
        proxy_pass http://linezolid:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}