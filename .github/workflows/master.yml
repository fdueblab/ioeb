name: Deploy to Server

on:
  push:
    branches:
      - deploy/ums

jobs:
  # 前端构建和推送镜像
  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install yarn
        run: npm install -g yarn

      - name: Install dependencies
        run: yarn install

      - name: Build frontend
        run: yarn build

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build and push frontend Docker image
        run: |
          docker build -t polarsnowleopard/ioeb-frontend:deploy_ums .
          docker push polarsnowleopard/ioeb-frontend:deploy_ums

  # Project_1 构建和推送镜像
  build-project1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build and push Project_1 Docker image
        run: |
          docker build -t polarsnowleopard/ioeb-project1:ums ./api/Project_1
          docker push polarsnowleopard/ioeb-project1:ums

  # Project_2 构建和推送镜像
  build-project2:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build and push Project_1 Docker image
        run: |
          docker build -t polarsnowleopard/ioeb-project2:ums ./api/Project_2
          docker push polarsnowleopard/ioeb-project2:ums

  # Project-3 构建和推送镜像
  build-project3:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build and push Project-3 Docker image
        run: |
          docker build -t polarsnowleopard/ioeb-project3:ums ./api/Project-3
          docker push polarsnowleopard/ioeb-project3:ums

  # Project-4 构建和推送镜像
  build-project4:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build and push Project-4 Docker image
        run: |
          docker build -t polarsnowleopard/ioeb-project4:ums ./api/Project-4
          docker push polarsnowleopard/ioeb-project4:ums
